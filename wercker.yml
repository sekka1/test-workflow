build:

  box:
    id: openjdk:8u111-jdk

  steps:
    - script:
        name: do something
        code: |
          apt-get update
          apt-get install -y curl zip

dothis:
  box:
    id: postgres:9.3.15

  steps:
    - script:
        name: Install gradle
        #cwd: all
        code: |
          apt-get update
          apt-get install -y curl zip

dosomethingelse:
  box:
    id: java:openjdk-8

  steps:
    - script:
        name: Install curl
        #cwd: all
        code: |
          apt-get update
          apt-get install -y curl zip

mysql:
  box:
    id: mysql:5.7

  steps:
    - script:
        name: Install curl
        #cwd: all
        code: |
          apt-get update
          apt-get install -y curl zip
